# 📘 راهنمای کامل پنل مدیریت اکانتینوکس

> آخرین به‌روزرسانی: تیر ۱۴۰۴  
> نسخه Django: 6.0.2 | Python 3.14

---

## فهرست مطالب

1. [نمای کلی و ساختار پنل](#1-نمای-کلی-و-ساختار-پنل)
2. [نقش‌ها و سطوح دسترسی](#2-نقشها-و-سطوح-دسترسی)
3. [داشبورد مرکزی](#3-داشبورد-مرکزی)
4. [ناوبار کناری (Sidebar)](#4-ناوبار-کناری-sidebar)
5. [مدیریت پشتیبانی](#5-مدیریت-پشتیبانی)
6. [مدیریت سفارش‌ها و مالی](#6-مدیریت-سفارشها-و-مالی)
7. [مدیریت محصولات](#7-مدیریت-محصولات)
8. [مدیریت بلاگ](#8-مدیریت-بلاگ)
9. [مدیریت محتوای سایت](#9-مدیریت-محتوای-سایت)
10. [تنظیمات سایت (فقط مالک)](#10-تنظیمات-سایت-فقط-مالک)
11. [مدیریت کاربران و گروه‌ها](#11-مدیریت-کاربران-و-گروهها)
12. [سفارشی‌سازی ناوبار و رابط کاربری](#12-سفارشیسازی-ناوبار-و-رابط-کاربری)
13. [راهنمای توسعه‌دهنده](#13-راهنمای-توسعهدهنده)

---

## 1. نمای کلی و ساختار پنل

پنل مدیریت اکانتینوکس یک **داشبورد سفارشی** بر پایه Django Admin است که شامل:

- **داشبورد عملیاتی** با آمار زنده (Real-time stats)
- **فضاهای کاری (Workspace Tabs)** برای دسترسی سریع
- **ناوبار کناری** با جستجوی فوری
- **نوار فرمان (Command Bar)** برای ناوبری سریع
- **سیستم علاقه‌مندی‌ها** (ستاره‌دار کردن صفحات)
- **تاریخچه آخرین صفحات** بازدید شده

### آدرس ورود
```
/admin/
```

### اپلیکیشن‌ها

| اپلیکیشن | نام فارسی | مدل‌ها |
|-----------|-----------|---------|
| `core` | هسته سایت | تنظیمات سایت، بنر اسلایدر، آمار اعتماد، کارت ویژگی، لینک فوتر، سوالات متداول |
| `accounts` | حساب‌های کاربری | پروفایل، کد OTP، آدرس سفارش |
| `shop` | فروشگاه | دسته‌بندی، محصول، سرویس، آیتم اکانت، سفارش، تراکنش |
| `blog` | بلاگ | پست بلاگ، سوالات متداول پست |
| `support` | پشتیبانی | جلسه گفتگو، پیام چت، مخاطب پشتیبانی، اشتراک پوش، حضور اپراتور، لاگ عملیات، امتیاز پشتیبانی |

---

## 2. نقش‌ها و سطوح دسترسی

سیستم نقش‌ها به صورت خودکار از **گروه‌های Django** تشخیص داده می‌شود:

| نقش | گروه | دسترسی |
|------|--------|---------|
| 🔴 **مالک (Owner)** | `Owner` یا Superuser | دسترسی کامل به همه بخش‌ها + تنظیمات سایت |
| 🟢 **پشتیبانی (Support)** | `Support Agent` | جلسات چت، مخاطبین، امتیازهای خود |
| 🔵 **CRM** | `CRM` | مخاطبین، خروجی اکسل، مدیریت جلسات |
| 🟡 **محتوا (Content)** | `Content` / `Editor` | پست‌های بلاگ، محتوای سایت |
| ⚪ **کارمند (Staff)** | هر کاربر `is_staff` | دسترسی محدود بر اساس permission‌ها |

### ایجاد نقش جدید
1. به **گروه‌ها و دسترسی** (`/admin/auth/group/`) بروید
2. گروه جدید با نام دقیق (`Owner`, `Support Agent`, `CRM`, `Content`) بسازید
3. Permission‌های مورد نیاز را تیک بزنید
4. کاربر مورد نظر را به گروه اضافه کنید

### محدودیت‌های خاص

| بخش | محدودیت |
|------|---------|
| تنظیمات سایت | فقط مالک/Superuser می‌تواند تغییر دهد |
| امتیازهای پشتیبانی | اپراتور فقط امتیازهای خود را می‌بیند؛ مالک همه را |
| خروجی اکسل مخاطبین | نیاز به `can_export_support_contacts` |
| حذف سفارش | فقط Superuser |

---

## 3. داشبورد مرکزی

داشبورد شامل ۵ بخش اصلی است:

### 3.1 کارت خوش‌آمدگویی
نمای کلی با پیام راهنما.

### 3.2 اقدام‌های سریع
بر اساس نقش شما لینک‌های فوری نمایش داده می‌شود:

- **مالک**: صندوق پشتیبانی + سفارش‌های در انتظار + پیش‌نویس بلاگ
- **پشتیبانی**: صندوق خوانده‌نشده + جلسات فعال + مخاطبین
- **CRM**: لیست مخاطبین + خروجی فیلترشده + جلسات بدون اپراتور
- **محتوا**: پیش‌نویس‌ها + همه پست‌ها + پست جدید

### 3.3 فضاهای کاری (Workspace Tabs)

| تب | محتوا |
|----|-------|
| 💬 **پشتیبانی** | صندوق خوانده‌نشده، گفتگوهای من، مخاطبین، امتیازها |
| 🛒 **سفارش‌ها** | در انتظار پرداخت، در بررسی، تراکنش‌های ناموفق |
| 📦 **محصولات** | لیست محصولات، سرویس‌ها، دسته‌بندی‌ها، آیتم‌های اکانت |
| ✍️ **بلاگ** | پیش‌نویس‌ها، پست جدید |
| 🎨 **محتوای سایت** | بنرها، کارت ویژگی‌ها، آمار اعتماد، لینک‌های فوتر، سوالات متداول |
| ⚙️ **تنظیمات** | (فقط مالک) تنظیمات سایت، کاربران، گروه‌ها، پروفایل‌ها |

هر تب شامل:
- **لینک‌های اقدام**: دکمه‌های میانبر
- **شمارنده‌ها**: آمار زنده از دیتابیس
- **نماهای پین‌شده**: قابلیت ذخیره فیلترهای سفارشی

### 3.4 تاریخچه صفحات
آخرین ۵ صفحه‌ای که در پنل مدیریت باز کرده‌اید (ذخیره در localStorage مرورگر).

### 3.5 علاقه‌مندی‌ها
مدل‌های پرکاربرد را ستاره‌دار کنید تا همیشه اینجا ظاهر شوند.

### 3.6 ساید‌بار — اقدامات اخیر
در ستون سمت چپ، آخرین ۱۰ عملیات شما (افزودن، ویرایش، حذف) نمایش داده می‌شود.

---

## 4. ناوبار کناری (Sidebar)

ناوبار کناری شامل تمام مدل‌های ثبت‌شده در ادمین است، گروه‌بندی شده بر اساس اپلیکیشن:

```
├── هسته سایت (core)
│   ├── بنرهای اسلایدر
│   ├── تنظیمات سایت
│   ├── سوالات متداول
│   ├── آمارهای اعتماد
│   ├── کارت‌های ویژگی
│   └── لینک‌های فوتر
├── حساب‌های کاربری (accounts)
│   ├── پروفایل‌ها
│   ├── کدهای OTP
│   └── آدرس‌های سفارش
├── فروشگاه (shop)
│   ├── دسته‌بندی‌ها
│   ├── محصولات
│   ├── سرویس‌ها
│   ├── آیتم‌های اکانت
│   ├── سفارش‌ها
│   └── تراکنش‌ها
├── بلاگ (blog)
│   ├── پست‌های بلاگ
│   └── سوالات متداول پست
├── پشتیبانی (support)
│   ├── جلسات گفتگو
│   ├── پیام‌های چت
│   ├── مخاطبین پشتیبانی
│   ├── اشتراک‌های پوش
│   ├── حضور اپراتورها
│   ├── لاگ‌های عملیات
│   └── امتیازهای پشتیبانی
└── Authentication
    ├── کاربران
    └── گروه‌ها
```

### فیلتر ناوبار
بالای ناوبار یک **فیلد جستجو** وجود دارد. تایپ کنید تا مدل‌ها فیلتر شوند.

### دکمه + (افزودن سریع)
کنار هر مدل دکمه **+** وجود دارد برای افزودن رکورد جدید.

### باز/بسته کردن گروه‌ها
روی نام هر اپلیکیشن کلیک کنید تا لیست مدل‌هایش باز/بسته شود.

---

## 5. مدیریت پشتیبانی

### 5.1 جلسات گفتگو (`ChatSession`)

**لیست جلسات:**

| ستون | توضیح |
|------|-------|
| مخاطب | نام و تلفن مشتری |
| آخرین پیام | خلاصه آخرین پیام با برچسب «کاربر» یا «اپراتور» |
| وضعیت | فعال / بسته |
| پیام خوانده‌نشده | تعداد پیام‌های خوانده‌نشده |
| اپراتور | اسم اپراتور اختصاص‌یافته |

**اقدامات سطری:**
- **باز کردن**: ورود به صفحه جزئیات جلسه
- **برداشت**: جلسه را به خود اختصاص دهید (فقط جلسات بدون اپراتور)
- **بستن**: بستن جلسه و علامت‌گذاری پیام‌ها به عنوان خوانده‌شده

**فیلترها:**
- وضعیت (فعال/غیرفعال)
- اپراتور
- پیام خوانده‌نشده (دارد/ندارد)
- تاریخ ایجاد

**اقدام دسته‌ای:**
- `بستن جلسات انتخاب‌شده` — بستن چندین جلسه همزمان

> 💡 **نکته**: اپراتورهای پشتیبانی وقتی بدون فیلتر وارد لیست می‌شوند، خودکار به صندوق خوانده‌نشده هدایت می‌شوند.

### 5.2 مخاطبین پشتیبانی (`SupportContact`)

لیست مخاطبینی که تیکت یا چت پشتیبانی داشته‌اند:

| ستون | توضیح |
|------|-------|
| تلفن | شماره نرمال‌شده |
| آخرین جلسه | لینک به آخرین جلسه چت |
| تعداد جلسات | کل جلسات این مخاطب |
| جلسه فعال | آیا جلسه فعال دارد |

**خروجی اکسل:**
- دکمه **Export selected contacts to Excel** — استخراج مخاطبین انتخاب‌شده
- آدرس **export-filtered/** — استخراج با فیلترهای اعمال‌شده
- شامل: تعداد جلسات، پیام‌های کاربر/اپراتور، آخرین فعالیت

> ⚠️ خروجی اکسل فقط برای Superuser یا کاربرانی با `can_export_support_contacts` permission.

### 5.3 امتیازهای پشتیبانی (`SupportRating`)

- اپراتورها فقط **امتیازهای خودشان** را می‌بینند
- مالک **همه امتیازها** را می‌بیند
- فیلتر بر اساس امتیاز (۱-۵) و تاریخ
- مالک فیلتر اضافی **بر اساس اپراتور** دارد

### 5.4 سایر مدل‌های پشتیبانی

| مدل | کاربرد |
|------|---------|
| پیام‌های چت | مشاهده و ویرایش پیام‌ها |
| اشتراک‌های پوش | مدیریت Push Notification subscriptions |
| حضور اپراتورها | مشاهده آخرین فعالیت اپراتورها |
| لاگ‌های عملیات | تاریخچه اقدامات (برداشت، بستن جلسه و ...) با IP و User-Agent |

---

## 6. مدیریت سفارش‌ها و مالی

### 6.1 سفارش‌ها (`Order`)

| ستون | توضیح |
|------|-------|
| مبلغ | مبلغ کل با فرمت تومان |
| اقلام | تعداد آیتم‌های سفارش |
| وضعیت سفارش | درحال بررسی / تأیید شد / تحویل داده شد / لغو شد |
| وضعیت پرداخت | پرداخت‌شده ✅ / در انتظار ⏳ / پرداخت ناموفق ❌ |
| تراکنش‌ها | لینک مستقیم به تراکنش‌های مرتبط |

**فیلترها:**
- وضعیت سفارش
- وضعیت پرداخت
- تاریخ (با date hierarchy)
- تراکنش ناموفق (دارد/ندارد)

**Inline:**
- آیتم‌های سفارش (محصول، قیمت، آیتم اکانت تخصیص‌یافته)

### 6.2 تراکنش‌ها (`TransactionLog`)

| ستون | توضیح |
|------|-------|
| سفارش | لینک به سفارش مرتبط |
| درگاه | نام درگاه پرداخت |
| نتیجه | موفق ✅ / ناموفق ❌ |
| تاریخ | تاریخ تراکنش |

> 💡 تراکنش‌ها فقط‌خواندنی هستند (ارسال پاسخ درگاه).

---

## 7. مدیریت محصولات

### 7.1 محصولات (`Product`)

| ستون | توضیح |
|------|-------|
| عنوان | نام محصول |
| دسته‌بندی | دسته محصول |
| سرویس | سرویس مرتبط |
| قیمت | قیمت با فرمت تومان |
| موجودی | تعداد آزاد / کل آیتم‌ها |
| تصویر | پیش‌نمایش thumbnail |

**ویرایش محصول:**
- اطلاعات محصول (عنوان، اسلاگ، دسته‌بندی، سرویس، توضیحات)
- تصویر (آپلود + پیش‌نمایش)
- قیمت
- SEO (عنوان SEO، توضیحات SEO) — section قابل باز/بسته

> 💡 اسلاگ از عنوان به صورت خودکار تولید می‌شود.

### 7.2 سرویس‌ها (`Service`)

سرویس‌ها دسته‌بندی سطح بالاتر محصولات هستند:

- **ویرایش درجا** (list_editable): فعال/غیرفعال و ترتیب
- هر سرویس لیست محصولاتش را به صورت inline نمایش می‌دهد

### 7.3 دسته‌بندی‌ها (`Category`)

- نام و اسلاگ
- تعداد محصولات هر دسته

### 7.4 آیتم‌های اکانت (`AccountItem`)

آیتم‌های رمزنگاری‌شده (نام‌کاربری/رمزعبور) که به محصولات تخصیص می‌یابند:

| وضعیت | معنی |
|--------|-------|
| آزاد ✅ | هنوز فروخته نشده |
| تخصیص‌یافته ⚠️ | به سفارشی تخصیص داده شده |

> ⚠️ اطلاعات رمزنگاری‌شده با FERNET_KEY محافظت می‌شوند.

---

## 8. مدیریت بلاگ

### 8.1 پست‌ها (`Post`)

| ستون | توضیح |
|------|-------|
| عنوان | عنوان پست |
| وضعیت | منتشرشده ✅ / پیش‌نویس ⚪ |
| تصویر | پیش‌نمایش تصویر شاخص |
| سوالات | تعداد FAQ مربوط به پست |

**ویرایش پست:**
- محتوای پست (عنوان، اسلاگ، محتوا)
- تصویر شاخص (با پیش‌نمایش بزرگ)
- وضعیت انتشار
- SEO (عنوان، توضیحات، کلمات کلیدی)
- سوالات متداول (inline — افزودن سوال/پاسخ)

**اقدامات دسته‌ای:**
- `انتشار پست‌های انتخاب‌شده`
- `برگرداندن به پیش‌نویس`

**فیلترها:**
- وضعیت انتشار
- بازه زمانی (این هفته / این ماه)
- تاریخ (با date hierarchy)

> 💡 کلمات کلیدی اگر خالی بمانند، به صورت خودکار از محتوای پست استخراج می‌شوند.

---

## 9. مدیریت محتوای سایت

این بخش شامل محتوای بصری و متنی صفحه اصلی لندینگ است:

### 9.1 بنرهای اسلایدر (`HeroBanner`)

مدیریت اسلایدهای هیروی صفحه اصلی:

- **عنوان** و **عنوان هایلایت** (بخش رنگی عنوان)
- **توضیحات**
- **بج/برچسب** (متن و رنگ)
- **دکمه‌ها** (تا ۲ دکمه با متن و لینک سفارشی)
- **تصویر پس‌زمینه** (اختیاری — اگر آپلود نشود گرادیان پیش‌فرض)
- **ترتیب** و **فعال/غیرفعال** — ویرایش درجا

### 9.2 کارت‌های ویژگی (`FeatureCard`)

بخش «چرا اکانتینوکس؟» صفحه اصلی:

- **آیکون**: رعد و برق، سپر، هدست، دلار، ستاره، قفل، موشک، هدیه
- **عنوان** و **توضیحات**
- **ترتیب** و **فعال/غیرفعال** — ویرایش درجا

### 9.3 آمار اعتماد (`TrustStat`)

ردیف اعداد اعتمادسازی (مثلاً «+۵۰۰۰ مشتری راضی»):

- **آیکون**، **مقدار**، **برچسب**
- **ترتیب** و **فعال/غیرفعال** — ویرایش درجا

### 9.4 لینک‌های فوتر (`FooterLink`)

لینک‌های پایین صفحه در ۲ ستون:

| ستون | نام |
|------|-----|
| `quick` | دسترسی سریع |
| `legal` | قوانین |

- **عنوان لینک**، **آدرس**
- **باز شدن در تب جدید**
- **ترتیب** و **فعال/غیرفعال** — ویرایش درجا

### 9.5 سوالات متداول (`GlobalFAQ`)

سوالات متداول عمومی سایت (نه FAQ پست‌های بلاگ):

- **سوال** و **پاسخ**
- **ترتیب** — ویرایش درجا

---

## 10. تنظیمات سایت (فقط مالک)

> ⚠️ **فقط کاربران با نقش Owner یا Superuser می‌توانند تنظیمات سایت را تغییر دهند.**  
> سایر کارمندان فقط می‌توانند تنظیمات را مشاهده کنند.

تنظیمات سایت یک **مدل تک‌نمونه (Singleton)** است — فقط یک رکورد وجود دارد و همیشه همان ویرایش می‌شود.

### بخش‌های تنظیمات

| بخش | فیلدها |
|------|---------|
| 🏢 **اطلاعات سایت** | نام سایت، نام تجاری فارسی، لوگو، توضیحات |
| 🎨 **رنگ‌ها و ظاهر** | رنگ اصلی، ثانویه، اکسنت، حالت Tailwind *(collapse)* |
| 📱 **اطلاعات تماس** | تلفن، ایمیل، اینستاگرام |
| 📣 **تلگرام** | لینک ادمین تلگرام، کانال، برچسب پشتیبانی |
| 🏠 **عناوین صفحه اصلی** | عناوین و زیرعناوین بخش‌های ویژگی‌ها و محصولات |
| 📢 **بخش CTA** | عنوان، توضیحات و ۲ دکمه فراخوانی |
| 🔗 **فوتر** | متن کپی‌رایت، نام و لینک توسعه‌دهنده |
| 🔐 **OTP / SMS** | فعال‌سازی OTP، زمان انقضا، حداکثر تلاش، کولداون، ارائه‌دهنده SMS *(collapse)* |
| 💳 **درگاه پرداخت** | انتخاب درگاه پرداخت *(collapse)* |
| 💬 **پشتیبانی** | حالت چت، ایمیل نوتیفیکیشن پشتیبانی *(collapse)* |
| ✅ **اینماد و نمادها** | کد HTML اینماد *(collapse)* |

> 💡 بخش‌های *(collapse)* به صورت پیش‌فرض بسته هستند. روی عنوان کلیک کنید تا باز شوند.

---

## 11. مدیریت کاربران و گروه‌ها

### 11.1 کاربران

مدیریت استاندارد Django User:
- تغییر رمز عبور
- فعال/غیرفعال کردن
- اضافه کردن به گروه‌ها
- تخصیص permission‌ها

### 11.2 گروه‌ها

گروه‌های موجود (یا باید ساخته شوند):

| گروه | کاربرد |
|------|--------|
| `Owner` | مالک سایت — دسترسی کامل |
| `Support Agent` | اپراتور پشتیبانی |
| `CRM` | مدیر ارتباط با مشتری |
| `Content` | تولیدکننده محتوا |
| `Editor` | ویراستار محتوا |

### 11.3 پروفایل‌ها

اطلاعات اضافی کاربران:
- شماره تلفن
- ایمیل و تاریخ عضویت (فقط‌خواندنی)

### 11.4 کدهای OTP

مشاهده وضعیت کدهای OTP ارسال‌شده:
- **فعال** ✅ — هنوز معتبر
- **منقضی** ⚪ — زمان گذشته
- **قفل شده** 🔴 — به دلیل تلاش‌های بیش از حد

---

## 12. سفارشی‌سازی ناوبار و رابط کاربری

### 12.1 تغییر نام سایت و برندینگ

در **تنظیمات سایت** فیلدهای زیر را ویرایش کنید:
- `نام سایت` → نمایش در عنوان صفحات
- `نام تجاری فارسی` → نمایش در هدر پنل ادمین
- `لوگو` → آپلود لوگو

**هدر ادمین** به صورت خودکار از `brand_wordmark_fa` یا `site_name` استفاده می‌کند.

### 12.2 نشانگر محیط (Environment Badge)

در گوشه بالای هدر:
- **DEBUG** (زرد) — حالت توسعه
- **PRODUCTION** (سبز) — حالت اجرا

این بر اساس `settings.DEBUG` تعیین می‌شود.

### 12.3 تم روشن/تیره

Django 6.0 از تم سیستم استفاده می‌کند. تم سفارشی اکانتینوکس:
- طراحی **Glassmorphism** با پالت **Teal**
- الهام از **Apple** و **Stripe**
- RTL کامل

### 12.4 نوار فرمان (Command Bar)

با فشردن **Ctrl+K** (یا کلیک روی آیکون جستجو) نوار فرمان باز می‌شود:
- تایپ کنید تا نتایج فیلتر شوند
- تمام مدل‌ها و لینک‌های مهم قابل جستجو هستند
- با **Enter** به اولین نتیجه بروید

### 12.5 علاقه‌مندی‌ها (Favorites)

- در لیست هر مدل، آیکون **⭐** را کلیک کنید تا به علاقه‌مندی‌ها اضافه شود
- علاقه‌مندی‌ها در داشبورد نمایش داده می‌شوند
- ذخیره در localStorage مرورگر (هر دستگاه جداگانه)

### 12.6 پین کردن نماها (Pinned Views)

در هر workspace tab می‌توانید فیلترهای سفارشی پین کنید:
1. به لیست مورد نظر بروید و فیلتر دلخواه خود را اعمال کنید
2. از منوی workspace روی **📌 Pin** کلیک کنید
3. فیلتر ذخیره شده در تب مربوطه ظاهر می‌شود

---

## 13. راهنمای توسعه‌دهنده

### 13.1 ساختار فایل‌های ادمین

```
apps/
├── core/
│   ├── admin.py           — SiteSettingsAdmin, HeroBannerAdmin, ...
│   ├── admin_site.py      — AccountinoxAdminSite (داشبورد، آمار، لینک‌ها)
│   └── apps.py            — AccountinoxAdminConfig
├── accounts/
│   └── admin.py           — ProfileAdmin, PhoneOTPAdmin, OrderAddressAdmin
├── shop/
│   └── admin.py           — ProductAdmin, OrderAdmin, TransactionLogAdmin, ...
├── blog/
│   └── admin.py           — PostAdmin + PostFAQInline
└── support/
    └── admin.py           — ChatSessionAdmin, SupportContactAdmin, ...

templates/admin/
├── index.html             — داشبورد سفارشی
├── base_site.html         — برندینگ و هدر
└── nav_sidebar.html       — ناوبار کناری سفارشی

static/admin/
├── custom.css             — استایل‌های سفارشی (~2800 خط)
└── admin.js               — JavaScript داشبورد (~900 خط)
```

### 13.2 افزودن مدل جدید به ادمین

```python
@admin.register(YourModel)
class YourModelAdmin(admin.ModelAdmin):
    list_display = ('field1', 'field2', ...)
    list_filter = ('filter_field',)
    search_fields = ('search_field',)
    fieldsets = (
        ('عنوان فارسی', {
            'fields': ('field1', 'field2'),
        }),
    )
```

> ⚠️ حتماً `verbose_name` و `verbose_name_plural` فارسی در `Meta` مدل تعریف کنید.

### 13.3 محدود کردن دسترسی به مدل

از `OwnerOnlyMixin` در `apps/core/admin.py` استفاده کنید:

```python
from apps.core.admin import OwnerOnlyMixin

@admin.register(SensitiveModel)
class SensitiveModelAdmin(OwnerOnlyMixin, admin.ModelAdmin):
    ...
```

### 13.4 افزودن آمار به داشبورد

در `apps/core/admin_site.py`، متد `_get_dashboard_stats` را ویرایش کنید:

```python
def _get_dashboard_stats(self, user):
    stats = {}
    ...
    # آمار جدید
    try:
        from apps.your_app.models import YourModel
        stats['your_stat_key'] = YourModel.objects.filter(...).count()
    except Exception:
        pass
    ...
    return stats
```

سپس در `templates/admin/index.html` از کلید استفاده کنید:
```html
<div><span>عنوان فارسی</span><strong>{{ admin_dashboard_stats.your_stat_key|default:"-" }}</strong></div>
```

### 13.5 افزودن لینک به Command Bar

در `admin_site.py`، لیست `link_defs` در `_get_command_links` را ویرایش کنید:

```python
link_defs = [
    ...
    ('admin:app_model_changelist', 'نام فارسی', 'کلمات کلیدی جستجو'),
]
```

### 13.6 افزودن تب جدید به Workspace

1. در `index.html` یک `<button>` جدید در `admin-workspace-tabs` اضافه کنید
2. یک `<article>` جدید با `data-workspace-panel="your_tab"` بسازید
3. `admin-workspace-actions` و `admin-workspace-counters` را پر کنید

### 13.7 سیستم نقش‌ها

نقش‌ها در `admin_site.py` → `_detect_role()` تعریف شده‌اند:

```python
def _detect_role(self, user):
    # اولویت: superuser → Owner group → Support Agent → CRM → Content/Editor → staff
```

برای افزودن نقش جدید:
1. یک گروه جدید در Django Admin بسازید
2. در `_detect_role` شرط جدید اضافه کنید
3. در `index.html` بخش `admin-role-priority` شرط `elif` اضافه کنید

---

## نکات مهم

1. **همیشه از verbose_name فارسی استفاده کنید** — هم در مدل و هم در فیلدست‌ها
2. **تنظیمات حساس فقط برای مالک** — از `OwnerOnlyMixin` استفاده کنید
3. **آمار زنده در داشبورد** — `_get_dashboard_stats` در هر بارگذاری صفحه اجرا می‌شود
4. **فیلترهای سفارشی** — از `admin.SimpleListFilter` استفاده کنید
5. **Badge وضعیت** — کلاس‌های CSS: `status-badge--success`, `status-badge--warning`, `status-badge--danger`, `status-badge--muted`
6. **format_html در Django 6.0** — حتماً از args/kwargs استفاده کنید نه f-string

---

*این مستند بخشی از پروژه اکانتینوکس است و برای استفاده تیم توسعه و مدیریت نوشته شده.*
