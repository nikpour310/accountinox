"""
PRODUCTION ENVIRONMENT VARIABLES CHECKLIST (G-5)

Before deploying to production, ensure all mandatory variables are set.
See docs/DEPLOY_CPANEL.md for detailed instructions.
"""

# === MANDATORY (Required in Production) ===

# Django secret key - Generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=change-me

# Debug mode - MUST be 0 in production (1 exposes secrets in error pages)
DEBUG=0

# Allowed domains - Set to your actual domain(s) in production
ALLOWED_HOSTS=localhost,127.0.0.1

# Public site URL used for canonical/OG absolute links (example: https://accountinox.ir)
SITE_URL=

# Support Push Notification (Web Push / VAPID)
SUPPORT_PUSH_ENABLED=1
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com

# === DATABASE ===

# MySQL on cPanel: mysql://username:password@localhost:3306/database_name
# SQLite default: sqlite:///db.sqlite3 (not recommended for production)
# DATABASE_URL=

# === SECURITY & ENCRYPTION ===

# Fernet key for encryption - Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=
# Enforce startup fail-fast when FERNET_KEY is missing/invalid (recommended: 1 in production)
REQUIRE_FERNET_KEY=1

# OTP HMAC key for production - MANDATORY in production (DEBUG=0)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
OTP_HMAC_KEY=

# === SSL/HTTPS (cPanel with proxy) ===

# If behind a proxy (CloudFlare, cPanel reverse proxy), set this:
# SECURE_PROXY_SSL_HEADER=HTTP_X_FORWARDED_PROTO,https
# Otherwise leave blank (direct HTTPS)
# SECURE_PROXY_SSL_HEADER=

# Force HTTPS redirect in production (usually True)
# SECURE_SSL_REDIRECT=1

# HSTS settings - Be careful! Start with conservative values
# SECURE_HSTS_SECONDS=3600
# SECURE_HSTS_INCLUDE_SUBDOMAINS=0
# SECURE_HSTS_PRELOAD=0

# CSRF trusted origins for cross-domain API calls (if applicable)
# CSRF_TRUSTED_ORIGINS=https://yourdomain.com,https://api.yourdomain.com

# === EXTERNAL SERVICES ===

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_SECRET=

# SMS provider (Kavenegar)
KAVENEGAR_API_KEY=

# IPPanel SMS provider (Edge API)
# Enable by setting SITE_SMS_PROVIDER=ippanel or via SiteSettings in admin
# See docs/OTP_IPPANEL_SETUP.md for full setup guide
#
# IPPANEL_API_KEY: API key from IPPanel panel > Developers > Access Keys
IPPANEL_API_KEY=
# IPPANEL_ORIGINATOR: Sender number (e.g. +983000505)
IPPANEL_ORIGINATOR=
# IPPANEL_PATTERN_CODE: Pattern code created in IPPanel for OTP (e.g. "abc123xyz")
IPPANEL_PATTERN_CODE=
# IPPANEL_SENDER: Legacy sender name (optional, used for plain-text SMS fallback)
IPPANEL_SENDER=

# === CACHING (Optional but recommended for production) ===

# Redis URL for caching (e.g., redis://localhost:6379/0)
# If not set, uses locmem (not suitable for multi-process production)
# REDIS_URL=

# === EMAIL (Optional for notifications) ===

# SMTP server configuration
# EMAIL_HOST=smtp.yourmailhost.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=your-email@yourdomain.com
# EMAIL_HOST_PASSWORD=your-email-password
# EMAIL_USE_TLS=1
# DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# === PAYMENT GATEWAY CREDENTIALS (Only if enabled) ===

# ZarinPal merchant ID (for Iranian payments)
# ZARINPAL_MERCHANT_ID=your-merchant-id

# Zibal merchant ID (alternative Iranian gateway)
# ZIBAL_MERCHANT_ID=your-merchant-id

# Payment mode: sandbox(1) / production(0)
PAYMENT_SANDBOX=1

# === PRODUCTION DEPLOYMENT CHECKLIST ===

# Before going live, verify:
# [ ] DEBUG=0 in production environment
# [ ] DJANGO_SECRET_KEY set to secure random value
# [ ] OTP_HMAC_KEY set (mandatory in production)
# [ ] FERNET_KEY set for encryption
# [ ] DATABASE_URL configured (MySQL or PostgreSQL)
# [ ] ALLOWED_HOSTS includes your production domain(s)
# [ ] SSL certificate installed (HTTPS working)
# [ ] SECURE_SSL_REDIRECT=1 in production
# [ ] Email SMTP configured (if notifications enabled)
# [ ] Static files collected (python manage.py collectstatic)
# [ ] Database migrations applied (python manage.py migrate)
# [ ] Healthcheck endpoint accessible (/healthz/)
# [ ] Superuser created (python manage.py createsuperuser)
# [ ] Logs directory writable and accessible
# [ ] Payment gateway credentials set (if e-commerce enabled)
# [ ] Google OAuth credentials configured (if social login enabled)
